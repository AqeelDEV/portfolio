// RGB triplets mirroring the @theme tokens in app/globals.css — for the
// inline-style, 2D-canvas, and WebGL call sites that can't read CSS
// variables. If a token changes in globals.css, change it here too (and the
// baked vec3 colors in HeroCanvasInner's GLSL shaders, which are the one
// place that can't consume these strings).
export const ACCENT_RGB = "40, 224, 208"; // --color-accent   #28e0d0
export const ACCENT_2_RGB = "139, 92, 246"; // --color-accent-2 #8b5cf6
export const MAGENTA_RGB = "226, 107, 245"; // --color-magenta  #e26bf5
